name: movie_people
description: "電影人 - 電影時刻表 App"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.6
  http: ^1.2.1
  html: ^0.15.4
  flutter_bloc: ^8.1.5
  get: ^4.6.6
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  intl: ^0.19.0
  firebase_core: 3.3.0
  firebase_auth: ^5.1.4
  google_sign_in: ^6.2.2
  firebase_database: ^11.0.4
  url_launcher: ^6.3.1
  geolocator: ^12.0.0
  geocoding: ^4.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - shorebird.yaml
